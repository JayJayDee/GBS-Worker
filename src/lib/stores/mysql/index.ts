import { createConnection } from 'typeorm';

const makeConnection =
  async () => {
    // TODO: create TypeORM-Mysql connection
    // createConnection({

    // })
  };

export const init =
  async () => {
  };
